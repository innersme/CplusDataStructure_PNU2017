/*

#include <stdio.h>

Matrix Add(Matrix b, int count, int b_count)
{
    Matrix res;
    idx = 0; int idx_b = 0;
    
    for (idx = 0 ; idx < count; idx++) {
        res.SMatrix[idx].Save(this->SMatrix[idx].col,this->SMatrix[idx].row,this->SMatrix[idx].value+7);
    }
    for (idx = count; idx < count + b_count; idx ++ ) {
        res.SMatrix[idx].Save(b.SMatrix[idx_b].col, b.SMatrix[idx_b].row, b.SMatrix[idx_b].value); idx_b++;
        
    }
    
    for (int i = 0; i < count + b_count ; i++) {
        res.SMatrix[i].print();
    }
    return res;
}



*/
